apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deployment
  namespace: app-baseline
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bachelor-app
  template:
    metadata:
      labels:
        app: bachelor-app
    spec:
      containers:
        - name: bachelor-app
          image: 065875800629.dkr.ecr.eu-central-1.amazonaws.com/idp-backend:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 4000   # anpassen, falls deine App einen anderen Port nutzt
          env:
            # Beispiel: falls du ENV Variablen brauchst
            - name: NODE_ENV
              value: "production"